// Função para alertar informações importantes (Exemplo de uso)
window.onload = function() {
    console.log("Página de dicas carregada com sucesso.");
};

// Exemplo de alerta inicial — pode ser removido se quiser
alert("⚠️ Confira as dicas para se manter seguro em situações de emergência!");
